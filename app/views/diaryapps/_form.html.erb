<%= form_with(model: diaryapp, local: true) do |form| %>
  <% if diaryapp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diaryapp.errors.count, "error") %> prohibited this diaryapp from being saved:</h2>

      <ul>
        <% diaryapp.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
